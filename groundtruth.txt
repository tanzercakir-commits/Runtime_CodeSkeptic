host: macos/aarch64+none
profile: macos-arm64-native.json

CASE                               PREDICTED     REALITY     PAIRING
------------------------------------------------------------------------------------------------------------
exact-mapping-in-carveout          UNSUPPORTED   refused     held
    mmap(0x1307200000, 131072, MAP_FIXED) failed: errno=12 ENOMEM (Cannot allocate memory)
exact-mapping-at-a-free-address    SUPPORTED     satisfied   held
    mmap(0x7000000000, 131072, MAP_FIXED) placed the mapping exactly there and it accepted reads and writes
rwx-anonymous-2mib                 UNSUPPORTED   refused     held
    mmap(PROT_READ|PROT_WRITE|PROT_EXEC) of 2097152 bytes failed: errno=13 EACCES (Permission denied)
page-size-at-most-4kib             UNSUPPORTED   refused     held
    sysconf(_SC_PAGESIZE) is 16384 and the build assumes 4096; os_page > PAGE, so the shipping check aborts the process
page-size-at-most-64kib            SUPPORTED     satisfied   held
    sysconf(_SC_PAGESIZE) is 16384 and the build assumes 65536; os_page <= PAGE, so the shipping check passes
file-map-beyond-eof                UNSUPPORTED   faulted     held
    reading past the end of the mapped file killed the process with signal 10 (Bus error: 10)
file-map-partial-page              SUPPORTED     satisfied   held
    the 16383 bytes between the end of a 1-byte file and the end of its final page read as zero, without faulting
write-then-execute-2mib            CONDITIONAL   satisfied   not asserted
    2097152 bytes mapped RW, written, flipped to RX with mprotect, and executed - W and X were never live together
hinted-mapping-requires-identity   UNSUPPORTED   relocated   held
    the hint 0x100948000 was occupied, so the kernel placed the mapping at 0x100968000 instead and reported SUCCESS - a program requiring guest/host identity has already lost here, and no error code told it
oversized-reservation-4pib         UNSUPPORTED   refused     held
    mmap of 4503599627370496 bytes (4096.0 TiB) was refused: errno=12 ENOMEM (Cannot allocate memory)
reserve-then-commit-1gib           SUPPORTED     satisfied   held
    1073741824 bytes reserved PROT_NONE, then one 16384-byte page inside it committed with mprotect and written
exact-mapping-below-minimum        UNSUPPORTED   refused     held
    mmap(0x800, 131072, MAP_FIXED) failed: errno=22 EINVAL (Invalid argument)
exact-mapping-above-user-space     UNSUPPORTED   refused     held
    mmap(0x7ffffe000000, 131072, MAP_FIXED) failed: errno=12 ENOMEM (Cannot allocate memory)
identity-without-translation       CONDITIONAL   relocated   not asserted
    the hint 0x1046bc000 was occupied, so the kernel placed the mapping at 0x1046dc000 instead and reported SUCCESS - a program requiring guest/host identity has already lost here, and no error code told it
pointer-truncation-32bit           UNSUPPORTED   lost        held
    the host returned a valid, writable mapping at 0x100b04000 (a sentinel round-tripped through the full pointer); stored in 32 bits it becomes 0xb04000, a different address the program can no longer reach. The kernel did nothing wrong - the caller's storage is too narrow for the address it was given
reservation-above-granted-1tib     SUPPORTED     satisfied   held
    the host actually reserved 1099511627776 bytes (1.0 TiB) of address space
exact-mapping-misaligned           UNSUPPORTED   misaligned-einval held
    aligned neighbour 0x7000000000 mapped and retained bytes; the target 0x7000000800 then failed with EINVAL
size-tail-must-stay-unmapped       UNSUPPORTED   tail-addressable held
    mmap requested 4097 bytes; bytes 4097 and 16383 were writable inside the host-rounded 16384-byte mapping
retry-permanent-exact-mapping      UNSUPPORTED   repeated-permanent-refusal held
    all 3 exact-mapping attempts reached the same structural EINVAL refusal (Invalid argument)
unhonourable-address-hint          SUPPORTED     satisfied-relocated held
    the unavailable hint 0x800 was ignored; mmap succeeded at 0x1004dc000 and the caller accepts relocation
unhonourable-carveout-hint         SUPPORTED     satisfied-relocated held
    the unavailable hint 0x1307200000 was ignored; mmap succeeded at 0x7000000000 and the caller accepts relocation
strong-reservation-alignment       CONDITIONAL   misaligned  not asserted
    ordinary mmap returned 0x104968000, which is not aligned to 1073741824 bytes
address-bound-below-minimum        UNKNOWN       skipped     skipped
    bounded below-minimum oracle is Linux-only

cases: 23   held: 19   contradicted: 0   not asserted: 4
